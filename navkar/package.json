{
  "name": "my-monorepo",
  "version": "1.0.0",
  "scripts": {
    "start": "concurrently --names \"FE,BE,ADM\" -c \"blue.bold,cyan.bold,green.bold\" \"npm run frontend:start\" \"npm run backend:start\" \"npm run admin:start\"",
    "frontend:start": "npm start --prefix navkar-frontend",
    "backend:start": "npm start --prefix navkar-backend",
    "admin:start": "npm run dev --prefix navkar-admin",
    "bootstrap": "npm install && npm install --prefix navkar-frontend && npm install --prefix navkar-backend && npm install --prefix navkar-admin"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
